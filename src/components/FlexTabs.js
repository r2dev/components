import React from 'react'
import styled from 'styled-components'
const FlexTabsContainer = styled.div`
  display: flex;
  width: 100%;
  height: 40px;
  z-index: 9;
  background: #fff;
`

const FlexTabs = ({values, selectedIndex=0, links}) => (
  <FlexTabsContainer>

  </FlexTabsContainer>
)

export default FlexTabs
